services:
  python-api:
    container_name: python-api
    build: 
      dockerfile: ./docker/dockerfile
    ports:
      - "8000:8000"
    volumes:
      - .:/code
    environment:
      - PYTHONPATH=/code
    

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080
volumes:
  postgres: 
    
    
